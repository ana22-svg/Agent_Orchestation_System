# Relay control-room prototype

Open `index.html` in a browser, or serve this folder with `python -m http.server 4173` from the `frontend` directory.

The prototype is intentionally deterministic for a portfolio/demo walkthrough. It demonstrates the Phase 6 interaction arc: live trace streaming, specialist handoff, reviewer send-back, approval escalation, and replay fork.
